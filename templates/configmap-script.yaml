apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vpProxyCa.fullname" . }}-script
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: {{ include "vpProxyCa.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  sync-proxy-ca.sh: |
{{ .Files.Get "files/sync-proxy-ca.sh" | indent 4 }}
