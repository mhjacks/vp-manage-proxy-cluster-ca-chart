---
# Hub namespace for the CronJob/Job, RBAC, script ConfigMap, and optional additional-ca Secret.
namespace: openshift-config

nameOverride: ""
fullnameOverride: ""

image:
  repository: quay.io/validatedpatterns/imperative-container
  tag: latest
  pullPolicy: IfNotPresent

# ConfigMap in openshift-config holding ca-bundle.crt for cluster Proxy trustedCA.
# Use a distinct name if another chart (e.g. opp-policy) already owns cluster-proxy-ca-bundle.
configMapName: vp-pattern-proxy-ca-bundle
targetNamespace: openshift-config

serviceAccount:
  create: true
  name: ""

# Run aggregation on a schedule; set suspend true to disable periodic runs.
cronJob:
  enabled: true
  suspend: false
  schedule: "*/10 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3

# One-shot Job on helm install/upgrade (runs after templates apply).
syncJob:
  enabled: true

# Optional: oc --selector when listing ManagedCluster objects (empty = all clusters).
managedClusterLabelSelector: ""
# Space-separated ManagedCluster names to skip. Default empty: every listed cluster receives the merged
# bundle and Proxy ManifestWork (hub is still updated directly by the job). Set e.g. local-cluster only if
# you intentionally skip that MC and rely on hub-only apply for it.
excludeManagedClusters: ""

# How to read trust material for each ManagedCluster:
# - spokePush (default): each spoke runs a CronJob (ManifestWork) that pushes trusted-ca-bundle (+ optional ingress)
#   into ConfigMaps in spokePush.hubNamespace on the hub; the gather job reads all bundle-* ConfigMaps.
# - acm: spec.managedClusterClientConfigs[].caBundle on ManagedCluster (hub API only; not each spoke's full platform bundle).
# - spokeTrustedCaBundle: fetch openshift-config-managed/trusted-ca-bundle via import kubeconfig.
managedClusterCaSource: spokePush

# When managedClusterCaSource is spokePush: hub namespace for per-cluster ConfigMaps (bundle-<cluster>).
spokePush:
  hubNamespace: vp-proxy-ca-bundles
  spokeNamespace: vp-proxy-ca-sync
  schedule: "*/10 * * * *"
  manifestWorkNameOverride: ""
  # Max duration for hub tokens embedded in spoke push Secrets (oc create token --duration).
  tokenDuration: 720h
  # Optional override for hub API URL in spoke kubeconfig; empty uses gather pod cluster server + CA.
  hubApiServer: ""

# Primary spoke distribution (default: all ManagedClusters except excludeManagedClusters; hub job lists clusters).
# - manifestwork: create/update ManifestWork in each managed cluster namespace (no kubeconfig). Independent of ACM Policy.
# - kubeconfig: use ACM import secret kubeconfigs (same as opp-policy-chart retrieval pattern).
distributeToSpokes: manifestwork

# ManifestWork metadata.name values (must be <=63 chars, DNS-1123).
manifestWork:
  nameOverride: ""
  proxyNameOverride: ""
  # Spokes should match the hub: set Proxy/cluster trustedCA to the merged bundle ConfigMap (default true).
  # Renders as the second manifest in the same ManifestWork as the bundle (strict apply order).
  # Legacy standalone Proxy ManifestWork name is MANIFEST_WORK_PROXY_NAME for delete-on-upgrade cleanup.
  patchClusterProxy: true

includeIngressCA: true

# Wait until ManagedClusterConditionAvailable=True before touching a spoke.
waitForManagedClusterAvailable: true
clusterReadinessMaxAttempts: 150
clusterReadinessSleepSeconds: 30

# Vault is expected on the hub: hub trusted-ca-bundle and ingress CA are gathered into
# the merge, so you normally do not add Vault's CA here. If Vault runs outside the hub (or
# presents a chain not already in that merged bundle), add the issuing CA PEM(s) below.
#
# Each list item is a YAML string. Use a block scalar (|) so PEM line breaks stay intact;
# you may concatenate multiple certs in one entry or use several list items.
#
# Example (commented out): ISRG Root X1 — a public root used by Let's Encrypt (replace with
# your CA as needed). To use: add an entry under additionalCaBundles with a block scalar (|),
# then paste the PEM lines (no leading # on those lines). Uncomment the YAML key lines below.
#
# additionalCaBundles:
#   - |
#     -----BEGIN CERTIFICATE-----
#     MIIFazCCA1OgAwIBAgIRAIIQz7DSQONZRGPgu2OCiwAwDQYJKoZIhvcNAQELBQAw
#     TzELMAkGA1UEBhMCVVMxKTAnBgNVBAoTIEludGVybmV0IFNlY3VyaXR5IFJlc2Vh
#     cmNoIEdyb3VwMRUwEwYDVQQDEwxJU1JHIFJvb3QgWDEwHhcNMTUwNjA0MTEwNDM4
#     WhcNMzUwNjA0MTEwNDM4WjBPMQswCQYDVQQGEwJVUzEpMCcGA1UEChMgSW50ZXJu
#     ZXQgU2VjdXJpdHkgUmVzZWFyY2ggR3JvdXAxFTATBgNVBAMTDElTUkcgUm9vdCBY
#     MTCCAiIwDQYJKoZIhvcNAQEBBQADggIPADCCAgoCggIBAK3oJHP0FDfzm54rVygc
#     h77ct984kIxuPOZXoHj3dcKi/vVqbvYATyjb3miGbESTtrFj/RQSa78f0uoxmyF+
#     0TM8ukj13Xnfs7j/EvEhmkvBioZxaUpmZmyPfjxwv60pIgbz5MDmgK7iS4+3mX6U
#     A5/TR5d8mUgjU+g4rk8Kb4Mu0UlXjIB0ttov0DiNewNwIRt18jA8+o+u3dpjq+sW
#     T8KOEUt+zwvo/7V3LvSye0rgTBIlDHCNAymg4VMk7BPZ7hm/ELNKjD+Jo2FR3qyH
#     B5T0Y3HsLuJvW5iB4YlcNHlsdu87kGJ55tukmi8mxdAQ4Q7e2RCOFvu396j3x+UC
#     B5iPNgiV5+I3lg02dZ77DnKxHZu8A/lJBdiB3QW0KtZB6awBdpUKD9jf1b0SHzUv
#     KBds0pjBqAlkd25HN7rOrFleaJ1/ctaJxQZBKT5ZPt0m9STJEadao0xAH0ahmbWn
#     OlFuhjuefXKnEgV4We0+UXgVCwOPjdAvBbI+e0ocS3MFEvzG6uBQE3xDk3SzynTn
#     jh8BCNAw1FtxNrQHusEwMFxIt4I7mKZ9YIqioymCzLq9gwQbooMDQaHWBfEbwrbw
#     qHyGO0aoSCqI3Haadr8faqU9GY/rOPNk3sgrDQoo//fb4hVC1CLQJ13hef4Y53CI
#     rU7m2Ys6xt0nUW7/vGT1M0NPAgMBAAGjQjBAMA4GA1UdDwEB/wQEAwIBBjAPBgNV
#     HRMBAf8EBTADAQH/MB0GA1UdDgQWBBR5tFnme7bl5AFzgAiIyBpY9umbbjANBgkq
#     hkiG9w0BAQsFAAOCAgEAVR9YqbyyqFDQDLHYGmkgJykIrGF1XIpu+ILlaS/V9lZL
#     ubhzEFnTIZd+50xx+7LSYK05qAvqFyFWhfFQDlnrzuBZ6brJFe+GnY+EgPbk6ZGQ
#     3BebYhtF8GaV0nxvwuo77x/Py9auJ/GpsMiu/X1+mvoiBOv/2X/qkSsisRcOj/KK
#     NFtY2PwByVS5uCbMiogziUwthDyC3+6WVwW6LLv3xLfHTjuCvjHIInNzktHCgKQ5
#     ORAzI4JMPJ+GslWYHb4phowim57iaztXOoJwTdwJx4nLCgdNbOhdjsnvzqvHu7Ur
#     TkXWStAmzOVyyghqpZXjFaH3pO3JLF+l+/+sKAIuvtd7u+Nxe5AW0wdeRlN8NwdC
#     jNPElpzVmbUq4JUagEiuTDkHzsxHpFKVK7q4+63SM1N95R1NbdWhscdCb+ZAJzVc
#     oyi3B43njTOQ5yOf+1CceWxG1bQVs5ZufpsMljq4Ui0/1lvh+wjChP4kqKOJ2qxq
#     4RgqsahDYVvTH9w7jXbyLeiNdd8XM2w9U/t7y0Ff/9yi0GE44Za4rF2LN9d11TPA
#     mRGunUHBcnWEvgJBQl9nJEiU0Zsnvgc/ubhPgXRR4Xq37Z0j4r7g1SgEEzwxA57d
#     emyPxgcYxn/eR44/KJ4EBs+lVDR3veyJm+kXQ99b21/+jh5Xos1AnX5iItreGCc=
#     -----END CERTIFICATE-----
#
# PEM blocks are merged into the bundle before de-duplication (each list entry is one or more certs).
# See README "Injecting extra CA material (additionalCaBundles)" for Git-friendly PEM injection and CNO/Vault notes.
# -- PEM strings merged into the gather bundle before fingerprint de-duplication; see README Overview "Injecting extra CA material".
additionalCaBundles: []

# ACM Policy + PlacementBinding (default on): Governance / console visibility for Proxy trustedCA vs configMapName.
# Recommended to keep enabled so ACM shows per-cluster compliance alongside ManifestWork rollout (Policy does not gate MW).
# If Placement selects zero clusters, hub Policy status may stay empty while MW still applies the bundle—fix Placement/bindings.
# The Job/CronJob owns ConfigMap content; Policy reports whether spokes keep Proxy trustedCA pointed at that ConfigMap name.
# When policy.createPlacement is true, the chart applies Placement (default cluster set "global").
# ManagedClusterSetBinding is optional (policy.createManagedClusterSetBindings); default false for GitOps.
# Set createPlacement false if you manage Placement yourself.
# Hub Policy status.complianceState stays empty while Placement selects zero clusters (propagator behavior).
policy:
  # -- Enable ACM Policy + PlacementBinding for Governance/GRC console visibility of Proxy trustedCA vs configMapName (does not gate ManifestWork).
  enabled: true
  # Base namespace for ACM policy workspace when placementRef.namespace is empty.
  hubNamespace: open-cluster-management
  # -- Hub Policy metadata.name; empty uses vpca-<truncated Release.Name> so len(namespace)+len(name)<=62 (ACM webhook).
  name: ""
  createPlacement: true
  # -- When false (default), chart does not render ManagedClusterSetBinding; bind policy.placement.clusterSets in the policy workspace via a platform GitOps app. Set true if this chart should own those bindings.
  createManagedClusterSetBindings: false
  placementRef:
    # Default Placement name created when policy.createPlacement is true; override if you use your own Placement.
    name: vp-proxy-ca
    # When non-empty, Policy, PlacementBinding, Placement, and ManagedClusterSetBinding use this namespace
    # (PlacementBinding cannot reference a Placement in another namespace). Empty uses policy.hubNamespace.
    namespace: ""
  placement:
    # ManagedClusterSets to bind into hubNamespace and to select from (requires clusters to be members of these sets).
    # Default "global" is the ACM default set that contains all managed clusters on typical installs.
    clusterSets:
      - global

resources:
  requests:
    cpu: 200m
    memory: 512Mi
  limits:
    cpu: "1"
    memory: 1Gi

# Pod-level security. runAsNonRoot requires a numeric runAsUser when the image USER is non-numeric (e.g. root).
# runAsUser must fall within your namespace SCC UID range (oc get namespace <ns> -o jsonpath='{.metadata.annotations.openshift\.io/sa\.scc\.uid-range}').
podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000690001
  runAsGroup: 0
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL
